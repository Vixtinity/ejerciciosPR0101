function ejercicio11() {
    let numeros = [];
    for (let i = 0; i < 100; i++) {
        let numerosaleatorios = Math.floor(Math.random() * 20) + 1; // 1 a 20
        numeros.push(numerosaleatorios);
    }
}
